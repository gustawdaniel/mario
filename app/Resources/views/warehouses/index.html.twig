{% extends 'base.html.twig' %}

{% block body %}
    <h1>Warehouses list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for warehouse in warehouses %}
            <tr>
                <td><a href="{{ path('crud_warehouses_show', { 'id': warehouse.id }) }}">{{ warehouse.id }}</a></td>
                <td>{{ warehouse.name }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('crud_warehouses_show', { 'id': warehouse.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('crud_warehouses_edit', { 'id': warehouse.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('crud_warehouses_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
