{% extends 'base.html.twig' %}

{% block body %}
    <h1>Warehouses</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ warehouse.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ warehouse.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('crud_warehouses_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('crud_warehouses_edit', { 'id': warehouse.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
