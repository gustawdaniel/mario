{% extends 'base.html.twig' %}

{% block body %}
    <h1>Phones</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ phone.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ phone.name }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ phone.number }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('crud_phones_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('crud_phones_edit', { 'id': phone.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
